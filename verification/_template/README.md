# Template — do not use directly

Create claim folders only via **init_folder.py** after **register.py**.

Target name: `<verification_dir>/YYYY-MM-DD-NN-CLAIM_ID/` (see registry.csv).

## Intent

What the change is supposed to improve — and how the gate measures the impact.

## Gates

Freeze thresholds a priori in run.py, before implementation starts.
